# PostgreSQL
DATABASE_URL="postgresql://dezix:dezix_password@localhost:5432/dezix?schema=public"

# Redis
REDIS_URL="redis://localhost:6379"

# NextAuth — 生产环境请使用 `openssl rand -base64 32` 生成
NEXTAUTH_SECRET="your-secret-here"
NEXTAUTH_URL="http://localhost:3000"

# 应用配置
NEXT_PUBLIC_APP_NAME="Dezix AI"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# 渠道 API Key 加密密钥 — 64 字符 hex (32 字节)
# 生成方式: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=""

# 上游 AI 供应商 API Key（用于 seed 脚本和网关转发）
OPENAI_API_KEY=""
ANTHROPIC_API_KEY=""
GOOGLE_API_KEY=""
DEEPSEEK_API_KEY=""
